# Sentinel Twin v2 — Python Dependencies
# Install with: pip install -r requirements.txt

# ── FastAPI Backend & WebSockets ────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.28.0
websockets>=12.0

# ── Sensor Communication ─────────────────────────────────────────
pyserial>=3.5              # Read ESP32 data over USB/UART serial
paho-mqtt>=2.0.0           # MQTT client for ESP32 via Raspberry Pi broker

# ── AI / HTTP ────────────────────────────────────────────────────
requests>=2.31.0           # HTTP calls to Gemini / Groq APIs

# ── Maths (Prediction) ───────────────────────────────────────────
numpy>=1.24.0              # Linear regression for trend prediction

# ── Config ───────────────────────────────────────────────────────
python-dotenv>=1.0.0       # Load API keys from .env file
psutil>=5.9.0
matplotlib>=3.7.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
